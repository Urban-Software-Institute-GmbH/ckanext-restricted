"resource_fields": [
  {
    "field_name": "restricted_schema_test",
    "label": "Schema Test Field (should appear)",
    "help_text": "If you see this, the scheming schema file is definitely in use.",
    "preset": "text"
  },
  {
    "field_name": "restricted",
    "label": "Access Restriction (SCHEMA ACTIVE ✅)",
    "help_text": "✅ If you see this message, scheming schema is active and ckanext-restricted UI is being rendered.",
    "preset": "composite",
    "subfields": [
      {
        "field_name": "level",
        "label": "Restriction Level",
        "preset": "select",
        "form_include_blank_choice": false,
        "choices": [
          {"value": "public", "label": "Public"},
          {"value": "registered", "label": "Registered users"},
          {"value": "only_allowed_users", "label": "Only allowed users"},
          {"value": "any_organization", "label": "Any organization member"},
          {"value": "same_organization", "label": "Same organization member"}
        ]
      },
      {
        "field_name": "allowed_users",
        "label": "Allowed Users",
        "preset": "user_autocomplete"
      }
    ]
  }
]
